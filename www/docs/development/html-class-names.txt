



.versiondisplay = a discrete invisible <span> that contains a resource label, for reference in support etc

--- IDs that might occur on any page ---
#repository = link to repository browser
#parent = go to parent folder
#history = link to log
#refresh = refresh the page
#logout = logout
#download = open up the browser's download box with the current item
#back = general do-nothing-and-go-to-last-page

#releaseversion = the version number of the release that the file comes from
#resourceversion = the revision number of the file
