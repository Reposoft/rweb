; repository paths that should be exported to local file system
; Use absolute paths, or paths starting with "admin" "html" "repo" or "backup"

; the ACL
administration/repos-access = admin/repos-access

; host startpage, start.html and contents
administration/startpage/ = html/
