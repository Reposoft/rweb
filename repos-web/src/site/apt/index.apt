The simple CMS reading plain HTML templates from WebDAV.

  Your subversion repository for the site can contain:
  * Plain HTML with relative URLs.
  * Absolute URLs to anything, including CSS stored elsewhere in the repository.
  
  Conditions
  * Keep everything within a root path in the repository
  * Make the repository available (read-only) with WebDAV
  